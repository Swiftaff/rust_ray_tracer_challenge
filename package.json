{
  "name": "rust_ray_tracer_challenge",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.rs",
  "scripts": {
    "watch_fmt_test": "cargo watch -d 4 -x fmt -x test",
    "watch_fmt_test_nocapture": "cargo watch -x \"test test_finding_n1_and_n2_at_various_intersections -- --nocapture\"",
    "watch_fmt_build": "cargo watch -d 4 -x fmt -x build",
    "run": "cargo run",
    "build": "cargo build",
    "format": "cargo fmt",
    "test": "cargo test",
    "fmt_build_run_current_program": "cargo fmt && cargo build && set RUST_BACKTRACE=1 && H:\\Development\\rust_ray_tracer_challenge\\target\\debug\\rust_ray_tracer_challenge.exe",
    "fmt_build_release_run_current_program": "cargo fmt && cargo build --release && H:\\Development\\rust_ray_tracer_challenge\\target\\release\\rust_ray_tracer_challenge.exe"
  },
  "commands": {
    "Command-Runner:rls restart ( Ctrl Shift R > Enter)": "${command:rls.restart}"
  },
  "author": "swiftaff",
  "license": "MIT"
}